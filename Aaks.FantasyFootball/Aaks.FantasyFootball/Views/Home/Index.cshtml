@{
    ViewBag.Title = "Players";
}

    <ul class="nav nav-tabs">
        <li class="nav-item">
            <a class="nav-link active" id="all-tab" data-toggle="tab" href="#all" role="tab" aria-controls="all" aria-selected="true">All</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" id="wr-tab" data-toggle="tab" href="#wr" role="tab" aria-controls="wr" aria-selected="true">WR</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" id="rb-tab" data-toggle="tab" href="#rb" role="tab" aria-controls="rb" aria-selected="true">RB</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" id="qb-tab" data-toggle="tab" href="#qb" role="tab" aria-controls="qb" aria-selected="true">QB</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" id="te-tab" data-toggle="tab" href="#te" role="tab" aria-controls="te" aria-selected="true">TE</a>
        </li>
    </ul>

    <div class="tab-content" id="tabs-by-positions">
        <div class="tab-pane fade show active" id="all" role="tabpanel" aria-labelledby="all-tab">
            <table class="table">

                <tr>
                    <th>Name</th>
                    <th>Position</th>
                    <th>Fantasy Poiints</th>
                    <th>2018 Auction Value</th>
          
                </tr>

                @foreach (var player in ViewBag.Players)
                {
                    if (player.IsKeeper)
                    {
                        <tr class="table-dark">
                            <td>@player.Name</td>
                            <td>@player.Position</td>
                            <td>@player.FantasyPoints</td>
                            @if (player.IsLastYearKeeper)
                            {
                                <td>@player.LastYearDraftValue (K)</td>
                            }
                            else
                            {
                                <td>@player.LastYearDraftValue</td>
                            }

          
                        </tr>
                    }
                    else
                    {
                        <tr>
                            <td>@player.Name</td>
                            <td>@player.Position</td>
                            <td>@player.FantasyPoints</td>
                            @if (player.IsLastYearKeeper)
                            {
                                <td>@player.LastYearDraftValue (K)</td>
                            }
                            else
                            {
                                <td>@player.LastYearDraftValue</td>
                            }

          
                        </tr>
                        
                     }


                            }

                    </table>
        </div>
        <div class="tab-pane fade" id="wr" role="tabpanel" aria-labelledby="wr-tab">
            <table class="table">

                <tr>
                    <th>Name</th>
                    <th>Position</th>
                    <th>Fantasy Poiints</th>
                    <th>2018 Auction Value</th>
          
                </tr>

                @foreach (var player in ViewBag.WideReceivers)
                {
                    if (player.IsKeeper)
                    {
                        <tr class="table-dark">
                            <td>@player.Name</td>
                            <td>@player.Position</td>
                            <td>@player.FantasyPoints</td>
                            @if (player.IsLastYearKeeper)
                            {
                                <td>@player.LastYearDraftValue (K)</td>
                            }
                            else
                            {
                                <td>@player.LastYearDraftValue</td>
                            }

          
                        </tr>
                    }
                    else
                    {
                        <tr>
                            <td>@player.Name</td>
                            <td>@player.Position</td>
                            <td>@player.FantasyPoints</td>
                            @if (player.IsLastYearKeeper)
                            {
                                <td>@player.LastYearDraftValue (K)</td>
                            }
                            else
                            {
                                <td>@player.LastYearDraftValue</td>
                            }

          
                        </tr>

                    }


                }

            </table>
        </div>

        <div class="tab-pane fade" id="rb" role="tabpanel" aria-labelledby="rb-tab">
            <table class="table">

                <tr>
                    <th>Name</th>
                    <th>Position</th>
                    <th>Fantasy Poiints</th>
                    <th>2018 Auction Value</th>
          
                </tr>

                @foreach (var player in ViewBag.RunningBacks)
                {
                    if (player.IsKeeper)
                    {
                        <tr class="table-dark">
                            <td>@player.Name</td>
                            <td>@player.Position</td>
                            <td>@player.FantasyPoints</td>
                            @if (player.IsLastYearKeeper)
                            {
                                <td>@player.LastYearDraftValue (K)</td>
                            }
                            else
                            {
                                <td>@player.LastYearDraftValue</td>
                            }

                        </tr>
                    }
                    else
                    {
                        <tr>
                            <td>@player.Name</td>
                            <td>@player.Position</td>
                            <td>@player.FantasyPoints</td>
                            @if (player.IsLastYearKeeper)
                            {
                                <td>@player.LastYearDraftValue (K)</td>
                            }
                            else
                            {
                                <td>@player.LastYearDraftValue</td>
                            }

                        </tr>

                    }


                }


            </table>
        </div>

        <div class="tab-pane fade" id="qb" role="tabpanel" aria-labelledby="qb-tab">
            
            <table class="table">

                <tr>
                    <th>Name</th>
                    <th>Position</th>
                    <th>Fantasy Poiints</th>
                    <th>2018 Auction Value</th>

                </tr>

                @foreach (var player in ViewBag.QuarterBacks)
                {
                    if (player.IsKeeper)
                    {
                        <tr class="table-dark">
                            <td>@player.Name</td>
                            <td>@player.Position</td>
                            <td>@player.FantasyPoints</td>
                            @if (player.IsLastYearKeeper)
                            {
                                <td>@player.LastYearDraftValue (K)</td>
                            }
                            else
                            {
                                <td>@player.LastYearDraftValue</td>
                            }


                        </tr>
                    }
                    else
                    {
                        <tr>
                            <td>@player.Name</td>
                            <td>@player.Position</td>
                            <td>@player.FantasyPoints</td>
                            @if (player.IsLastYearKeeper)
                            {
                                <td>@player.LastYearDraftValue (K)</td>
                            }
                            else
                            {
                                <td>@player.LastYearDraftValue</td>
                            }


                        </tr>

                    }


                }


            </table>
            
        </div>
        <div class="tab-pane fade" id="te" role="tabpanel" aria-labelledby="te-tab">
            <table class="table">

                <tr>
                    <th>Name</th>
                    <th>Position</th>
                    <th>Fantasy Poiints</th>
                    <th>2018 Auction Value</th>

                </tr>

                @foreach (var player in ViewBag.TightEnds)
                {
                    if (player.IsKeeper)
                    {
                        <tr class="table-dark">
                            <td>@player.Name</td>
                            <td>@player.Position</td>
                            <td>@player.FantasyPoints</td>
                            @if (player.IsLastYearKeeper)
                            {
                                <td>@player.LastYearDraftValue (K)</td>
                            }
                            else
                            {
                                <td>@player.LastYearDraftValue</td>
                            }


                        </tr>
                    }
                    else
                    {
                        <tr>
                            <td>@player.Name</td>
                            <td>@player.Position</td>
                            <td>@player.FantasyPoints</td>
                            @if (player.IsLastYearKeeper)
                            {
                                <td>@player.LastYearDraftValue (K)</td>
                            }
                            else
                            {
                                <td>@player.LastYearDraftValue</td>
                            }

                        </tr>

                    }


                }


            </table>
        </div>

    </div>

    


    